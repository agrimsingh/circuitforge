# Anthropic API key (required)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Vercel Blob for shareable export artifacts
# BLOB_READ_WRITE_TOKEN=

# Optional: Vercel Sandbox local auth (populated by `vercel env pull`)
# VERCEL_OIDC_TOKEN=

# Optional: Convex persistent error-memory backend (for adaptive self-learning across deploys)
# CONVEX_SITE_URL=https://<your-deployment>.convex.site
# Optional fallback accepted by CircuitForge if produced by Convex CLI:
# NEXT_PUBLIC_CONVEX_SITE_URL=https://<your-deployment>.convex.site
# CIRCUITFORGE_CONVEX_SHARED_SECRET=replace-me
